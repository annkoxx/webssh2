services:
  - type: web
    name: webssh
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: /app/webssh -p 10000
    envVars:
      - key: PORT
        value: "10000"
      - key: WEBSSH_ADMIN_USER
        value: "admin"
      - key: WEBSSH_ADMIN_PASSWORD
        sync: false
